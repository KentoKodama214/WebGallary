spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/web_gallary_test
    username: postgres
    password: postgres
  sql:
    init:
      mode: always
  jackson:
    time-zone: Asia/Tokyo

auth:
  login:
    failCount: 3

app:
  photo:
    photoCountPerPage: 20
    maxFileSizeMb: 5
    miniUserUpperLimit: 10
    normalUserUpperLimit: 20
    outputPath: https://www.xxx.com/